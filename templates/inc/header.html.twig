<header>
    <nav>
        <ul>
            <a href="{{ path('main_home') }}"><img src="{{ asset('img/CatToken.png') }}"></a>
            {% if app.user %}
                <li><a href="#">Administration</a></li>
                <li><a href="{{ path('main_home') }}">Accueil</a></li>
                <li><a href="{{ path('profil_home', {'id': app.user.id}) }}">Mon profil</a></li>
                <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
<main>
