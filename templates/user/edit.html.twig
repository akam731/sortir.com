{% extends 'base.html.twig' %}

{% block title %}Modification Utilisateur{% endblock %}

{% block head %}
{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}


    <a href="{{ path('admin_user') }}">back to list</a>

    {{ include('user_crud/_delete_form.html.twig') }}
{% endblock %}
